apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
        {{- include "common.labels" . | indent 4 }}
  annotations:
    kubernetes.io/ingress.class: traefik
    external-dns.alpha.kubernetes.io/target: {{ .Values.ingress.loadBalancerIP }}
  name: timetable-import-info-v1-timetable
  namespace: {{ .Release.Namespace }}
spec:
  rules:
          {{- if eq .Values.env "production"}}
    - host: timetable-import-info-v1-timetable-{{ .Release.Namespace }}.entur.io
          {{ else }}
    - host: timetable-import-info-v1-timetable-{{ .Release.Namespace }}.{{ .Release.Namespace }}.entur.io
            {{- end}}
      http:
        paths:
          - pathType: ImplementationSpecific
            backend:
              service:
                name: {{ template "nisaba.name" . }}
                port:
                  number: {{ .Values.service.http.externalPort }}
